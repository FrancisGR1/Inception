services:

  nginx:
    container_name: nginx
    depends_on:
      - wordpress
    build:
      context: ./nginx
    volumes:
      - wordpress_data:/var/www/wordpress
    ports:
      - "443:443"
    restart: always
    networks:
      - inception-network

  mariadb:
    container_name: mariadb
    build:
      context: ./mariadb
    volumes:
      - mariadb_data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    restart: always
    networks:
      - inception-network

  wordpress:
    container_name: wordpress
    depends_on:
      - mariadb
    build:
      context: ./wordpress
    volumes:
      - wordpress_data:/var/www/wordpress
    environment:
      DB_HOST: mariadb
      DB_PASSWORD: ${MYSQL_PASSWORD}
      DB_NAME: ${MYSQL_DATABASE}
      DB_USER: ${MYSQL_USER}
      WORDPRESS_SITE_URL: ${WORDPRESS_SITE_URL}
    restart: always
    networks:
      - inception-network

networks:
  inception-network:
    driver: bridge

volumes:
  mariadb_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/frmiguel/data/mariadb
  wordpress_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/frmiguel/data/wordpress
